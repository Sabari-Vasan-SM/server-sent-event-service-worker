{
  "name": "server-sent-event-service-worker",
  "version": "1.0.0",
  "description": "- **Server** sends increasing counter every 2 seconds (via SSE)\r - **Frontend** displays live count\r - **Service worker** caches app files for offline support\r - If offline → UI still loads (but live updates stop)",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Sabari-Vasan-SM/server-sent-event-service-worker.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Sabari-Vasan-SM/server-sent-event-service-worker/issues"
  },
  "homepage": "https://github.com/Sabari-Vasan-SM/server-sent-event-service-worker#readme",
  "dependencies": {
    "express": "^5.2.1"
  }
}
